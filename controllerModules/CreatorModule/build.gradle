repositories {
   flatDir {
       dirs '../lib'
   }
}

dependencies {
	compile project(":SkeletonModule")
    compile files('../SkeletonModule/bin')
    compile name: 'json'
} 


clean.doFirst {
	delete "bin/"
	println "CreatorModule/bin/ deleted"
}