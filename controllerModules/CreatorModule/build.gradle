repositories {
   flatDir {
       dirs '../lib'
   }
}

dependencies {
	compile project(":MapModule")
    compile files('../MapModule/bin')
    compile files('../SkeletonModule/bin')
    compile name: 'json'
} 


clean.doFirst {
	delete "bin/"
	println "CreatorModule/bin/ deleted"
}