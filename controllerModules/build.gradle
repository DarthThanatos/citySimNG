subprojects {
	apply plugin: 'java'
	sourceSets {
		main {
			java {
				srcDirs = ['src']
			}
			output.classesDir = "bin"
		}
	}
	repositories {
    flatDir {
       dirs '../SystemMainController/lib'
	   }
 	}

	dependencies {
	   compile name: 'json'
	}
}
